# Employment Application System — environment configuration
# Copy values as needed; the server auto-loads this file on start.

# ---- Telegram notifications (send XLSX + summary on each submission) ----
# 1. Create a bot via @BotFather on Telegram and copy its token.
# 2. Get your chat/group ID (e.g. via @userinfobot or by adding the bot to a group).
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ---- Optional overrides ----
# PORT=3000
# SESSION_SECRET=change-me-in-production
# REF_PREFIX=EMP